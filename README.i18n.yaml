name: '@leizhuang/sky-axis'
description:
  en: 'Browser-only DSH web GUI plugin: developer workspace sidebar with collapsible icon rail, two-level menu, 5 views (home / team / personal / requirements / reports / settings), requirement CRUD + SSE.'
  zh: '纯浏览器 DSH Web GUI 插件：开发工作台 sidebar + 可折叠 icon rail + 二级目录 + 5 视图 + 需求 CRUD/SSE。'
category: ui
tags:
  - sky-axis
  - sidebar
  - workspace
bundle:
  patch: ./cordis.patch.yml
client:
  platform: web
  inject:
    - '@deepseek-ai/dsh-client-runtime'
